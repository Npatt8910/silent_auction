<!DOCTYPE html>
<html>
<head>
  <title>Silent Auction: Nicole’s Car</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>🚗 Bid on 2010 Toyota Vena</h1>
  <p>This isn’t just a car. It’s a vessel of stories, spilled coffee, and road trips.</p>
  <img src="https://via.placeholder.com/400x250" alt="Nicole's Car">

  <form action="/submit-bid" method="POST">
    <label>Name: <input type="text" name="name" required></label><br>
    <label>Email: <input type="email" name="email" required></label><br>
    <label>Bid Amount ($): <input type="number" name="amount" required></label><br>
    <button type="submit">Submit Silent Bid</button>
  </form>
</body>
</html>
<script>
  document.querySelector('form').addEventListener('submit', function(e) {
    const minBid = <%= car.minBid %>;
    const bidAmount = parseFloat(document.querySelector('input[name="amount"]').value);

    if (bidAmount < minBid) {
      alert(`Bid must be at least $${minBid}`);
      e.preventDefault();
    } else if ((bidAmount - minBid) % 25 !== 0) {
      alert('Bids must increase in $25 increments');
      e.preventDefault();
    }
  });
</script>

